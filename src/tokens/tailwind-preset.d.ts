/**
 * Typings for the generated Tailwind preset (`god-kit/tailwind/preset`).
 * Copied to `dist/tailwind/preset.d.ts` during post-build.
 */
declare const preset: {
  readonly theme: {
    readonly extend: Record<string, unknown>
  }
}

export default preset
